# نقشهٔ صفر تا صد ShiftSafe به زبان ساده

## هدف ساده

ShiftSafe قرار است بررسی کند یک مدل AI روی دادهٔ جدید چقدر قابل اعتماد است. اگر شرایط داده عوض شد، سیستم باید بتواند بگوید:

- هنوز می‌توانیم به مدل اعتماد کنیم؛
- اول باید مدل یا داده را اصلاح کنیم؛
- مدل باید از پیش‌بینی خودداری کند.

این پروژه در نسخهٔ اول گواهی حقوقی، ایمنی، درآمد یا compliance نمی‌دهد.

## مسیر مرحله‌به‌مرحله

### مرحلهٔ صفر: تعریف دقیق مسئله

فقط یک نوع داده انتخاب می‌کنیم: دادهٔ جدولی/سری‌زمانی سنسوری.

خروجی این مرحله:

- تعریف target؛
- تعریف زمان؛
- تعریف شرایط تغییر داده؛
- تعریف هزینهٔ خطا.

معیار عبور: بتوانیم مسئله را در یک جمله توضیح دهیم.

### مرحلهٔ یک: دروازهٔ کیفیت داده

کد فعلی همین مرحله است. بررسی می‌کند:

- ستون‌های لازم وجود دارند یا نه؛
- مقدار هدف خالی است یا نه؛
- دادهٔ تکراری وجود دارد یا نه؛
- timestamp قابل تبدیل است یا نه؛
- زمان مرتب است یا نه.

تصمیم فعلی فقط `CONTINUE`, `REWORK`, `STOP` است.

### مرحلهٔ دو: baseline

قبل از مدل پیچیده، یک baseline ساده می‌سازیم. اگر مدل پیچیده از baseline بهتر نباشد، دلیلی برای ادامه نداریم.

### مرحلهٔ سه: split درست

سه حالت را مقایسه می‌کنیم:

1. random split؛
2. temporal split؛
3. group/site/device split.

هدف، جلوگیری از نتیجهٔ مصنوعی ناشی از leakage است.

### مرحلهٔ چهار: عدم‌قطعیت

سه روش را آزمایش می‌کنیم:

- conformal prediction؛
- quantile/prediction intervals؛
- ensemble یا Bayesian approximation.

برای هر روش می‌سنجیم interval چقدر پوشش واقعی دارد و چقدر پهن است.

### مرحلهٔ پنج: distribution shift

داده را عمداً به چند وضعیت تقسیم می‌کنیم:

- فصل جدید؛
- سایت جدید؛
- سنسور جدید؛
- operating regime جدید.

اگر مدل در این وضعیت‌ها overconfident شود، گزارش باید آن را نشان دهد.

### مرحلهٔ شش: abstention

مدل نباید همیشه مجبور به پاسخ‌دادن باشد. اگر uncertainty زیاد است، باید بگوید:

> این نمونه برای پیش‌بینی قابل اعتماد نیست؛ بررسی انسانی یا دادهٔ بیشتر لازم است.

### مرحلهٔ هفت: تصمیم هزینه‌محور

فقط accuracy کافی نیست. برای هر کاربرد باید بدانیم:

- false alarm چقدر هزینه دارد؛
- missed event چقدر هزینه دارد؛
- abstention چقدر قابل قبول است.

خروجی باید به تصمیم عملی وصل شود.

### مرحلهٔ هشت: گزارش پژوهشی

گزارش باید شامل این بخش‌ها باشد:

- مسئله؛
- داده؛
- روش؛
- آزمایش؛
- نتیجه؛
- failure case؛
- محدودیت؛
- سؤال بعدی.

این گزارش برای استاد مهم‌تر از ظاهر dashboard است.

### مرحلهٔ نه: بستهٔ تجاری

نسخهٔ تجاری اولیه:

> ارزیابی مستقل قابلیت اعتماد یک مدل AI روی یک dataset و یک شرایط جدید.

تحویل:

- PDF مدیریتی؛
- JSON فنی؛
- notebook بازتولیدپذیر؛
- جلسهٔ توضیح؛
- یک iteration محدود.

### مرحلهٔ ده: اعتبارسنجی بازار

قبل از SaaS، با ۱۵ مخاطب صحبت می‌کنیم:

- ۵ شرکت صنعتی یا انرژی؛
- ۵ آژانس AI؛
- ۵ استاد یا آزمایشگاه.

ادامه فقط وقتی مجاز است که حداقل دو گفت‌وگوی واقعی و یک درخواست داده، proposal یا قیمت داشته باشیم.

### مرحلهٔ یازده: مسیر فاند

سه نسخه از یک پروژه می‌سازیم:

- KAUST: uncertainty quantification در انرژی/محیط‌زیست و سری‌زمانی؛
- MBZUAI: trustworthy statistical ML و calibration؛
- Khalifa: reliability در AI صنعتی، انرژی و آب.

MVP خودش permit نیست. MVP باید باعث شود استاد یا گروه، تو را برای proposal، research assistantship یا پذیرش funded جدی بگیرد.

## چیزهایی که فعلاً نمی‌سازیم

- مدل foundation؛
- سرویس cloud بزرگ؛
- محصول multi-tenant؛
- اتصال ERP؛
- certification؛
- ادعای ROI؛
- پشتیبانی از همهٔ صنایع.

## معیارهای موفقیت

### فنی

- اجرای کامل روی دادهٔ عمومی؛
- تست‌های سبز؛
- بازتولیدپذیری؛
- تشخیص failure عمدی؛
- گزارش قابل خواندن.

### پژوهشی

- سؤال واضح؛
- baseline معتبر؛
- مقایسهٔ روش‌ها؛
- آزمایش shift؛
- limitation صادقانه؛
- امکان مقاله یا proposal.

### تجاری

- مصاحبه با خریدار واقعی؛
- دریافت use case؛
- درخواست قیمت؛
- paid pilot؛
- زمان تحویل قابل کنترل برای یک نفر.

## قانون توقف

اگر بعد از انتشار MVP و ۱۵ تماس هدفمند هیچ استاد، شرکت یا آژانسی حاضر به گفت‌وگو، دادن use case یا درخواست قیمت نبود، feature اضافه نمی‌کنیم. پروژه باید تغییر دامنه یا متوقف شود.
